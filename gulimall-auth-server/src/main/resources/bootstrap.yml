spring:
  application:
    name: gulimall-auth-server
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
    sentinel:
      transport:
        #配置sentinel dashboard地址
        dashboard: localhost:8080

#暴露所有端点
management:
  endpoints:
    web:
      exposure:
        include: '*'